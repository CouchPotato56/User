name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Cache Maven local repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2-

      - name: Build and run tests
        run: mvn -B -V test package

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: userservice-artifact
          path: target/*.jar

  security:
    name: Security scan
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Install Snyk CLI
        run: |
          curl -sSL https://static.snyk.io/cli/latest/snyk-linux -o /usr/local/bin/snyk
          chmod +x /usr/local/bin/snyk

      - name: Check Snyk configuration
        id: snyk-check
        run: |
          if [ -z "${SNYK_TOKEN}" ]; then
            echo "Snyk token not configured; skipping security scan."
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          else
            echo "Snyk token configured; running security scan."
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Run Snyk security scan
        if: steps.snyk-check.outputs.enabled == 'true'
        continue-on-error: true
        run: |
          export JAVA_HOME="$(dirname $(dirname $(readlink -f $(which java))))"
          export PATH="$JAVA_HOME/bin:$PATH"
          snyk test --all-projects --severity-threshold=high
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
